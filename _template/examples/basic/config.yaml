meta:
  environment: production
  region: us-east-1  # Replace with appropriate region
  name: example
  team: platform
  cost_center: eng-001

# Add resource-specific configuration here
resource:
  field: value

security:
  encryption_enabled: true
  public_access: false
  deletion_protection: true
  audit_logging: true

tags:
  example: basic
